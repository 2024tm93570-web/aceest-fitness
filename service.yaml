apiVersion: v1
kind: Service
metadata:
  name: acest-fitness-service
spec:
  selector:
    app: acest-fitness
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30001
